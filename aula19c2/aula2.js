/* Variáveis

    const (valor constante, não pode ser mudado)
    let (valor da variável pode ser alterado)
    var (descontinuado, não é recomendado usar)

*/

const abacate = "Já se inscreveu?"

console.log(abacate)